// Importamos React
import React from 'react';

const Error = () => {
    // Metodo render (Se encargara de mostrar la vista al usuario)
    return (
            <section id="content">
                <h2 className="subheader">Pagina no encontrada</h2>
                <p>La pagina a la que intenta acceder no existe en la WEB</p>            
            </section>
    )
}
// Lo exportamos 
export default Error;